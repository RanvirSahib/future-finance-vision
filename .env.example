# Money Lens frontend configuration
# Use mock data while the Python backend is being developed.
VITE_USE_MOCK_DATA=true
VITE_API_URL=http://localhost:8000

# Compatibility aliases if another environment expects NEXT_PUBLIC_* names.
NEXT_PUBLIC_USE_MOCK_DATA=true
NEXT_PUBLIC_API_URL=http://localhost:8000
